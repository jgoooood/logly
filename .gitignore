node_modules
.next
out
build
dist

# 환경변수 (모두 차단, 예시만 허용)
.env*
!.env.example
!.env.sample

.DS_Store
*.log
.vscode
.idea

next-env.d.ts

# typescript incremental cache
*.tsbuildinfo

# vercel (Stage 8 배포 시 생성)
.vercel

# test coverage (도입 시)
coverage
.nyc_output

# Claude Code 머신별 권한 캐시
.claude/settings.local.json

# Supabase CLI 로컬 캐시 (linked-project, cli-latest 등)
supabase/.temp

# 인증서 / 개인키 / 키스토어
*.pem
*.key
*.p12
*.pfx

# 클라우드 자격증명 (혹시 모를 실수 차단)
*-credentials.json
*-service-account.json
.aws
